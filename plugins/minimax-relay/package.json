{
  "name": "harness-desktop-minimax-relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Forward MiniMax requests with reasoning_split set, so thinking arrives in its own field.",
  "main": "lib/index.js",
  "exports": {
    ".": { "default": "./lib/index.js" },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
